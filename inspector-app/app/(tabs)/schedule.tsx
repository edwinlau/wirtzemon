import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function ScheduleScreen() {
  return (
    <PhasePlaceholder
      phase="Phase 2"
      title="Schedule"
      description="Import .ics calendar invites from REA/Domain, see your inspections in chronological order, and get travel-time clash warnings between back-to-back open homes."
    />
  );
}
