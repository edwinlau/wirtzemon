import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function InspectScreen() {
  return (
    <PhasePlaceholder
      phase="Phase 5"
      title="Inspect"
      description="Start a room-by-room inspection: capture photos, record voice notes, answer prompted questions, and track your progress — all resumable if the app closes."
    />
  );
}
