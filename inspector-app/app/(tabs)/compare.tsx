import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function CompareScreen() {
  return (
    <PhasePlaceholder
      phase="Phase 7"
      title="Compare"
      description="Review every inspected property side by side — plain-language summaries, photo confidence badges, your ratings, pros/cons and completion scores — and filter by what matters."
    />
  );
}
